services:
  mongo:
    container_name: hw0-mongo
    image: mongo:7
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: shop
    volumes:
      - ./init.js:/docker-entrypoint-initdb.d/init.js
      - ./queries:/queries
